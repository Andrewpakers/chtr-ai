import Layout from "../components/layout";
export default function Chat() {
    return (
        <Layout>
            <main>
                <span>This is the chat page</span>
            </main>
        </Layout>
    );
}